#requirement numbers maybe deleted due to incompatability
pandas==2.0.3
scipy==1.11.1
streamlit==1.25.0
altair == 5.0.1
plotly == 5.15.0

# To find the current code thats compatible from VS
# Please clear all version numbers and run the streamlit
# Environment. Then put this code in the env. 
# conda activate <your-env-name>
# then:
# conda list --export > requirements.txt